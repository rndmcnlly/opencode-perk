# npm package: installed deps and compiled output (built on publish).
node_modules/
dist/

# Opt-in self-demo: a local symlink that loads perk on this repo (see README).
.opencode/

# Legacy captures from perk <= 0.2; current versions use OpenCode's temp dir.
.perk/

# Field notes from self-test runs: working-tree scratch, never a tracked artifact.
FEEDBACK.md
